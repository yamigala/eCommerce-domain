/**
 * @author Yamini Gala
 * @prop id:unique identifier
 * @prop itemName: string type
 * @prop name:string type
 * @prop noOfItem:string type
 * @prop address:string type
 * @prop phoneNo:string type
 */

export class order {
  id: number;
  itemName: string;
  name: string;
  noOfItem: number;
  price: number;
  address: string;
  phoneNo: number;
}
